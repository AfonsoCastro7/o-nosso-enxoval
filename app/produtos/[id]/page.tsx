/* eslint-disable @next/next/no-img-element -- local compressed data URLs */
"use client";
import { use, useState } from "react";
import { useRouter } from "next/navigation";
import { Calendar, ExternalLink, ImageIcon, Pencil, ShoppingBag, Store, Trash2 } from "lucide-react";
import { CategoryBadge, PriorityBadge } from "@/components/Badges";
import { ProductForm } from "@/components/ProductForm";
import { LoadingState, PageHeader } from "@/components/ui";
import { useProducts } from "@/hooks/useProducts";
import { formatCurrency, formatDate } from "@/lib/format";
export default function ProductPage({ params }: { params: Promise<{ id: string }> }) { const { id } = use(params); const router = useRouter(); const { products, ready, editProduct, removeProduct } = useProducts(); const [editing, setEditing] = useState(false); const [buying, setBuying] = useState(false); const product = products.find((p) => p.id === id); if (!ready) return <LoadingState/>; if (!product) return <><PageHeader title="Produto não encontrado" back="/"/><p className="text-slate-500">Este produto já não existe.</p></>; if (editing) return <><PageHeader title="Editar produto" back={`/produtos/${id}`}/><ProductForm initial={product} submitLabel="Guardar alterações" onSubmit={(input) => { editProduct({ ...product, ...input }); setEditing(false); }}/></>; const remove = () => { if (window.confirm(`Eliminar “${product.name}”? Esta ação não pode ser anulada.`)) { removeProduct(id); router.push(product.status === "bought" ? "/comprados" : "/desejos"); } }; return <><PageHeader title={product.name} subtitle={product.status === "bought" ? "Produto comprado" : "Na tua lista de desejos"} back={product.status === "bought" ? "/comprados" : "/desejos"}/><div className="mb-5 flex h-72 items-center justify-center overflow-hidden rounded-3xl bg-slate-100 text-slate-400">{product.image ? <img src={product.image} alt={product.name} className="h-full w-full object-cover"/> : <ImageIcon size={42}/>}</div><div className="card p-5"><div className="flex flex-wrap gap-2"><CategoryBadge category={product.category}/><PriorityBadge priority={product.status === "wishlist" ? product.priority : undefined}/></div>{product.description && <p className="mt-5 leading-7 text-slate-600">{product.description}</p>}<div className="mt-6 grid grid-cols-2 gap-4"><Info label={product.status === "bought" ? "Preço pago" : "Preço atual"} value={formatCurrency(product.price)}/>{product.targetPrice !== undefined && <Info label="Preço alvo" value={formatCurrency(product.targetPrice)}/>}<Info label="Quantidade" value={String(product.quantity)}/>{product.store && <Info label="Loja" value={product.store} icon={<Store size={15}/>}/>} {product.purchaseDate && <Info label="Data da compra" value={formatDate(product.purchaseDate)} icon={<Calendar size={15}/>}/>}</div>{product.notes && <div className="mt-6 border-t border-slate-100 pt-5"><p className="label">Notas</p><p className="whitespace-pre-wrap text-sm leading-6 text-slate-600">{product.notes}</p></div>}{product.url && <a href={product.url} target="_blank" rel="noreferrer" className="button-secondary mt-5 w-full">Ver produto <ExternalLink size={17}/></a>}</div>{product.status === "wishlist" && <button onClick={() => setBuying(true)} className="button-primary mt-4 w-full"><ShoppingBag size={18}/>Marcar como comprado</button>}{buying && <BuyPanel onCancel={() => setBuying(false)} onSave={(price, store, purchaseDate) => { editProduct({ ...product, status: "bought", price, store: store || product.store, purchaseDate }); setBuying(false); }}/>}<div className="mt-4 grid grid-cols-2 gap-3"><button className="button-secondary" onClick={() => setEditing(true)}><Pencil size={17}/>Editar</button><button className="button-danger" onClick={remove}><Trash2 size={17}/>Eliminar</button></div></>; }
function Info({ label, value, icon }: { label: string; value: string; icon?: React.ReactNode }) { return <div><p className="label">{label}</p><p className="flex items-center gap-1.5 font-semibold text-slate-800">{icon}{value}</p></div>; }
function BuyPanel({ onCancel, onSave }: { onCancel: () => void; onSave: (price: number, store: string, date: string) => void }) { const [price, setPrice] = useState(""); const [store, setStore] = useState(""); const [date, setDate] = useState(new Date().toISOString().slice(0, 10)); return <div className="card mt-4 space-y-4 border border-teal-100 p-5"><h2 className="font-bold">Concluir compra</h2><label><span className="label">Preço pago *</span><input autoFocus required type="number" min="0" step="0.01" value={price} onChange={(e) => setPrice(e.target.value)}/></label><label><span className="label">Loja</span><input value={store} onChange={(e) => setStore(e.target.value)}/></label><label><span className="label">Data da compra</span><input type="date" value={date} onChange={(e) => setDate(e.target.value)}/></label><div className="flex gap-3"><button className="button-secondary flex-1" onClick={onCancel}>Cancelar</button><button disabled={price === ""} className="button-primary flex-1 disabled:opacity-50" onClick={() => onSave(Number(price), store, date)}>Guardar</button></div></div>; }
